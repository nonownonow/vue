// const path = require('path')
const webpack = require('webpack')
// const config = require('../config')
// const webpackConfig = require('./webpack.prod.conf')

console.log(process.env.NODE_ENV)
webpack({

}, (err, stats)=>{
  if(err||status.hasErros()){

  }
})
